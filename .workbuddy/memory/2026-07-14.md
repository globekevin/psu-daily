# 2026-07-14 工作日志

## PSU Daily News 第 24 期（自动化触发 7:00 AM）
- **状态**: ✅ 成功
- **6 条新闻**:
  1. 传媒学院: 31 名 Bellisario 本科生获 Discovery Grants 暑期研究资助 (psu.edu, 7/13)
  2. 演出预告: Don Toliver「NITROUS」巡演 9/2 登陆 BJC (psu.edu, 6/23)
  3. 校友活动: Smeal 商学院两位华尔街教授退休，Nittany Lion Fund 创始人 (psu.edu, 6/26)
  4. 体育动态: Tony Rojas ACL 伤后恢复超预期，力量教练盛赞 (SI.com, 7/9)
  5. 行政人事: 校董会 7/16-17 Penn College 召开，审议战略与预算 (psu.edu, 6/15)
  6. 科研成果: 12 项 GAP 基金各 $75K 推动校园科研成果商业化 (psu.edu, 6/22)
- **图像**: 全部 6 条有 og:image — 4 psu-gatsby-files S3, 1 minutemediacdn, 1 generic campus
- **去重**: 全部 6 条 URL 为全新，与 history.json 114 条无冲突
- **文件**: history.json (114 entries), index.html, psu-news-2026-07-14.html, archive.html, archive-catalog.html
- **Git**: 未提交（7:30 AM 单独自动化负责）
- **效率**: 13 次工具调用

## 10:53 移动端横向滚动修复（用户截图反馈）
- **报告**: iPhone 13 Pro 截图显示页面底板超出屏幕宽度，左右滑动露出右侧黑色背景
- **根因**: (1) 微信 WebView viewport 报告不准（"请求桌面版"或缩放），`@media (max-width: 429px)` 没生效，按桌面布局渲染 (2) `html/body` 无 `overflow-x:hidden` 兜底，子元素撑宽 body
- **额外 bug**: 构建脚本 `<span{source_class} class="source-badge">` 生成两个 `class` 属性，每篇日报 5 处受影响
- **修复**:
  - `index.html` + `psu-news-2026-07-14.html`: 加 `html, body { overflow-x: hidden; max-width: 100%; }` + 网格容器 `min-width: 0` + `.card-summary` 加 `word-break: break-word` + 修复 5 处重复 class 属性
  - `build_20260714.py`: 第 152 行合并为单一 `class` 属性
- **提交**: `c7a57d6 Fix mobile horizontal scroll: add overflow-x:hidden to html/body, merge duplicate class attributes`
- **推送**: origin main 成功，等待 GitHub Pages 部署（1-2 分钟）

## 11:07 Footer 改手风琴（用户截图反馈）
- **报告**: 第二张截图（11:04）显示移动端 footer 仍是 3 列横排，"核心关键词"等被截到屏幕外
- **用户诉求**: 改成 3 行手风琴 — 默认只显示标题，点击才展开内容（"关于本日报"/"每日筛选标准"/"核心关键词"）
- **实现**:
  - HTML：每列改成 `<button class="footer-toggle" aria-expanded="false" aria-controls="fc-N">` + `<div class="footer-content" hidden>` 结构
  - CSS：`.footer-grid` 从 `grid 2fr 1fr 1fr` 改为 `flex column`，加按钮 + `▸`/`▾` 旋转动画 + `fadeIn` 动画
  - JS：监听所有 `.footer-toggle` 点击，切换 `aria-expanded` 和 `hidden`
  - 暗黑模式：调整 h4 颜色、border 颜色、icon 颜色
  - 删掉 3 处响应式 `.footer-grid { grid-template-columns: 1fr 1fr }` 等覆盖（不再需要）
- **提交**: `c15afd0 Footer: collapse 3 columns into accordion (3 stacked rows, click to expand)`
- **效果**: 移动端 footer 紧凑成 3 个全宽标题，点击后向下展开内容；无 JS 时 `hidden` 属性也能保证只显示标题
